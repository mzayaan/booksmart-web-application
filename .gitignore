# Visual Studio local state
.vs/
*.user
*.suo

# Build output
bin/
obj/
*.dll
*.pdb

# NuGet — restored with `nuget restore` / `msbuild -t:restore`
packages/

# Local secrets — copy Web.config and fill in your own values
Web.local.config
*.publishsettings
*.pubxml
